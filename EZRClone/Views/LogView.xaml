<UserControl x:Class="EZRClone.Views.LogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:EZRClone.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:LogViewModel}"
             Background="{DynamicResource BackgroundBrush}">
    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
        <TextBlock Text="Log" FontSize="22" FontWeight="SemiBold"
                   Foreground="{DynamicResource ForegroundBrush}"
                   HorizontalAlignment="Center" Margin="0,0,0,16" />
        <TextBlock Text="{Binding PlaceholderText}" Foreground="{DynamicResource ForegroundSecondaryBrush}"
                   TextAlignment="Center" TextWrapping="Wrap" MaxWidth="400" />
    </StackPanel>
</UserControl>
