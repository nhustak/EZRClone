<Window x:Class="EZRClone.Views.DownloadProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Download Progress"
        Width="720"
        Height="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        Background="{DynamicResource BackgroundBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Download Progress"
                   FontSize="24"
                   FontWeight="SemiBold"
                   Foreground="{DynamicResource ForegroundBrush}" />

        <Border Grid.Row="1"
                Margin="0,16,0,12"
                Padding="16"
                Background="{DynamicResource SurfaceBrush}"
                CornerRadius="4">
            <StackPanel>
                <TextBlock x:Name="OverallStatusText"
                           FontSize="14"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource ForegroundBrush}"
                           TextWrapping="Wrap" />
                <TextBlock x:Name="CurrentItemText"
                           Margin="0,8,0,0"
                           FontSize="13"
                           Foreground="{DynamicResource ForegroundSecondaryBrush}"
                           TextWrapping="Wrap" />
                <TextBlock x:Name="TargetText"
                           Margin="0,6,0,0"
                           FontSize="12"
                           Foreground="{DynamicResource ForegroundSecondaryBrush}"
                           TextWrapping="Wrap" />
            </StackPanel>
        </Border>

        <Border Grid.Row="2"
                Margin="0,0,0,12"
                Padding="14"
                Background="{DynamicResource BackgroundSecondaryBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="4">
            <StackPanel>
                <TextBlock Text="Live rclone status"
                           FontSize="12"
                           Foreground="{DynamicResource ForegroundSecondaryBrush}" />
                <TextBlock x:Name="SpeedEtaText"
                           Margin="0,8,0,0"
                           FontSize="15"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource AccentBrush}"
                           TextWrapping="Wrap" />
                <TextBlock x:Name="ProgressLineText"
                           Margin="0,8,0,0"
                           FontSize="12"
                           Foreground="{DynamicResource ForegroundBrush}"
                           TextWrapping="Wrap" />
            </StackPanel>
        </Border>

        <TextBlock Grid.Row="3"
                   Text="Recent output"
                   Margin="0,0,0,8"
                   FontSize="12"
                   Foreground="{DynamicResource ForegroundSecondaryBrush}" />

        <TextBox x:Name="OutputTextBox"
                 Grid.Row="4"
                 IsReadOnly="True"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 TextWrapping="Wrap"
                 FontFamily="Consolas"
                 FontSize="12"
                 Padding="10"
                 Background="{DynamicResource SurfaceBrush}"
                 Foreground="{DynamicResource ForegroundBrush}" />

        <Grid Grid.Row="5"
              Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="CompletionText"
                       VerticalAlignment="Center"
                       Foreground="{DynamicResource ForegroundSecondaryBrush}"
                       TextWrapping="Wrap" />

            <Button x:Name="CloseButton"
                    Grid.Column="1"
                    Content="Close"
                    Padding="18,8"
                    Click="OnClose"
                    IsEnabled="False" />
        </Grid>
    </Grid>
</Window>
